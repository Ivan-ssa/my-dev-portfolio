document.addEventListener("DOMContentLoaded", () => {
    const projects = document.querySelectorAll(".project");

    projects.forEach(project => {
        project.classList.add("fade-in");
    });
});

// Código para qualquer interação adicional no futuro
